
export interface Service {
  icon: React.ReactNode;
  title: string;
  description: string;
}

export interface Testimonial {
  image: string;
  name: string;
  handle: string;
  quote: string;
}

export interface FAQItem {
  question: string;
  answer: string;
}
